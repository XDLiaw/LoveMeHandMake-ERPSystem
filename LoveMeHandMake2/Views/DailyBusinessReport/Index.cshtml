@model LoveMeHandMake2.Models.ViewModels.DailyBusinessReportViewModel

@{
    ViewBag.Title = "营业日报表";
}

<h2>营业日报表</h2>


@using (Html.BeginForm("Index", "DailyBusinessReport", FormMethod.Post))
{
    <div class="container breadcrumb">
        <div class="col-md-3">
            @Html.DropDownList("SearchStoreID", (IEnumerable<SelectListItem>)ViewBag.StoreList, htmlAttributes: new { id = "SearchStoreID", @class = "form-control" })
        </div>
        <div class="col-md-3">
            @Html.EditorFor(model => model.SearchDateStart, new { htmlAttributes = new { id = "SearchDateStart", name = "SearchDateStart", @class = "form-control calendarBox", placeholder = "日期(起)" } })
        </div>
        <div class="col-md-3">
            @Html.EditorFor(model => model.SearchDateEnd, new { htmlAttributes = new { id = "SearchDateEnd", name = "SearchDateEnd", @class = "form-control calendarBox", placeholder = "日期(起)" } })
        </div>
        <div class="col-md-1">
            <input type="submit" value="搜寻" class="btn btn-default" />
        </div>
        <div class="col-md-2">
            <input type="button" value="产出Excel报表" class="btn btn-file btn-primary" onclick="btnDownload();" />
        </div>
    </div>
}

<table class="table table-striped table-hover ">
    <thead>
        <tr class="">
            <th>@Html.DisplayNameFor(model => model.DailyRecords.FirstOrDefault().Date)</th>
            <th>@Html.DisplayNameFor(model => model.DailyRecords.FirstOrDefault().Cash)</th>
            <th>@Html.DisplayNameFor(model => model.DailyRecords.FirstOrDefault().CreditCard)</th>
            <th>@Html.DisplayNameFor(model => model.DailyRecords.FirstOrDefault().MallCard)</th>
            <th>@Html.DisplayNameFor(model => model.DailyRecords.FirstOrDefault().Total)</th>
        </tr>
    </thead>
    <tbody>
        @if (Model.DailyRecords != null)
        {
            foreach (var item in Model.DailyRecords)
            {
                <tr>
                    <td>@Html.DisplayFor(modelItem => item.Date)</td>
                    <td>@Html.DisplayFor(modelItem => item.Cash)</td>
                    <td>@Html.DisplayFor(modelItem => item.CreditCard)</td>
                    <td>@Html.DisplayFor(modelItem => item.MallCard)</td>
                    <td>@Html.DisplayFor(modelItem => item.Total)</td>
                </tr>
            }
        }
    </tbody>
    <tfoot>
        <tr class="">
            <td colspan="4">@Html.DisplayNameFor(model => model.TotalMoney)</td>
            <td>@Html.DisplayFor(model => model.TotalMoney)</td>
        </tr>
    </tfoot>
</table>




@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")

    <script type="text/javascript">
        function btnDownload() {
            try {
                var storeID = $("#SearchStoreID").val();
                var start = $("#SearchDateStart").val();
                var end = $("#SearchDateEnd").val();
                window.location.href = "/DailyBusinessReport/DownloadReport?SearchStoreID=" + storeID + "&SearchDateStart=" + start + "&SearchDateEnd=" + end;
                return true;
            }
            catch (e) {
                return false;
            }
        }
    </script>
}